/target

specs/
plan/

# BIOS files (copyrighted, must not be committed)
*.bin
*.BIN
SCPH*.BIN
SCPH*.bin

# Environment configuration (keep .env.example)
.env

# CPU trace logs (generated during debugging)
bios_trace.log
*.trace.log